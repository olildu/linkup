# Auto-merged by `docker compose up` for local dev: hot-reload + live source mount.
services:
  backend:
    volumes:
      - .:/app
    command: sh -c "python migrate.py && uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload"
